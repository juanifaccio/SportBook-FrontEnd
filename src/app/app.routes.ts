import { Routes } from '@angular/router';
import { TipoCanchaComponent } from './components/tipo-cancha/tipo-cancha';

export const routes: Routes = [
  {
    path: 'tipos-cancha',
    component: TipoCanchaComponent
  }
];