/**
 * Breakpoints de la aplicación. La cátedra exige que la app se vea correctamente
 * en al menos tres: SM, MD y LG. Los valores están alineados con los de Angular
 * Material / CDK para no tener dos escalas conviviendo.
 *
 * El CSS se escribe mobile-first: el estilo base es el de pantalla chica y estas
 * media queries solo agregan reglas hacia arriba.
 */
export const BREAKPOINT_SM = '(min-width: 600px)';
export const BREAKPOINT_MD = '(min-width: 960px)';
export const BREAKPOINT_LG = '(min-width: 1280px)';
